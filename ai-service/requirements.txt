# ai-service/requirements.txt
# FIX: All versions pinned so future installs don't pull breaking changes.
# FIX: torch and torchvision removed — they added ~2 GB but predict_fake()
#      was never called. Re-add if/when you integrate the ResNet detector.

fastapi==0.109.2
uvicorn[standard]==0.27.1
python-multipart==0.0.9
pydantic==2.6.1
pydantic-settings==2.2.1

# TensorFlow / Keras for the denoising model
tensorflow==2.16.1
# keras is bundled with tensorflow>=2.12, no separate install needed

# Image processing
Pillow==10.2.0
numpy==1.26.4
opencv-python-headless==4.9.0.80

# OCR
pytesseract==0.3.10

# Security
python-dotenv==1.0.1
